h1.m-t-xl.text-center.color-white class=("#{'color-white' if controller?('landing_pages') }")
  strong = "Leader Board"
.row
  .col-3
  .col-6.mt-5.font-size-12
    table.table.table-striped.table-bordered.table-hover.shade-primary
      thead
        tr
          th Trend
          th #
          th Icon
          th Name
          th Level
          th Status
          th Balance
          th Trades
      tbody
        - User.leaders.each.with_index do |user, index|
          tr
            td class='#{trend_color(TREND_NO_CHANGE)} text-white text-center fa fa-arrow-circle-#{TREND_NO_CHANGE}' style="display: table-cell" 
            td = index + 1
            td.icon-leaderboard  = image_tag("wizard.png")
            td = user.name
            td = user.level
            td = user.level_label
            td = '%.2f %s' % [user.overall_balance, CASH_CURRENCY]
            td = user.trades